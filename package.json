{
  "name": "legaldocs",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered legal document platform for GCC - Arabic, English & Urdu",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "deploy:api": "cd apps/api && npm run deploy",
    "deploy:web": "cd apps/web && npm run pages:deploy",
    "db:create": "cd apps/api && wrangler d1 create legaldocs-db",
    "db:migrate": "cd apps/api && wrangler d1 execute legaldocs-db --file=./schema.sql",
    "db:migrate:local": "cd apps/api && wrangler d1 execute legaldocs-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.0.0"
}
